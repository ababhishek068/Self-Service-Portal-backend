HIJRA — BUSINESS CENTRAL ADMIN STEPS (Baby Steps)
=================================================
Do these on the BC DEV machine (Felix / AL) AFTER portal deploy.
Publish each change: Compile → Publish extension → restart BC service if asked.

A) HR SERVICE REQUEST LETTERS (Guarantee, Experience, Mortgage, etc.)
----------------------------------------------------------------------
1. Open BC AL project (Felix repo) in VS Code
2. Confirm codeunit CuPortalHrLetters (52110) exists and compiles
3. Publish the extension to HIJRA UAT company
4. On portal server .env add (or check):
   BC_SOAP_LETTERS_CODEUNIT_URL=http://10.30.7.14:2448/.../CuPortalHrLetters
5. Test: Portal → HR → Service Request Letters → submit one letter → should NOT say "service missing"

B) EMPLOYEE EXIT (Transfer, Resignation, Exit Interview)
--------------------------------------------------------
1. Confirm codeunit CuPortalEmployeeExit (52101) is published
2. On portal .env:
   BC_SOAP_EXIT_CODEUNIT_URL=.../CuPortalEmployeeExit
3. Test: Portal → HR → Employee Exit → create Transfer Request → appears in list

C) TRAINING NEED ASSESSMENT (Department, Period, Provider, Cost)
-----------------------------------------------------------------
1. Publish codeunit CuPortalTraining (companion to FnTrainingRequest)
2. Test: create training request → list shows Department + Period columns
3. If course error: ensure Employee No. is stamped on training header (AL fix in FnTrainingRequest)

D) LEAVE BALANCE & LEAVE STATEMENT
----------------------------------
1. Publish CuPortalEmployeeData with method GetLeaveBalance
2. Publish FnGetEmployeeLeaveBalances on staff portal codeunit
3. On QyHREmployee OData page expose:
   - Annual Leave balance / Earned Leave Days / Leave Balance
   - EmployeeContractType (employment type)
   - JobGrade
4. Test: Leave Request → balance matches employee card in BC

E) APPROVAL REJECTION REASONS
-----------------------------
1. Publish OData query QyApprovalCommentLine (table Approval Comment Line)
   Fields: Document_No, Approval_Entry_No, Comment
2. Test: reject a request in BC → requester sees rejection reason in portal

F) STAFF CLAIM TYPES (only Medical Refund showing)
--------------------------------------------------
1. BC → Receipts and Payment Types
2. For each claim type needed: set Type = Claim
3. Portal will list them automatically (no portal code change)

G) PETTY CASH DEPARTMENT LIMIT
------------------------------
1. Create/publish OData query QyPettyCashLimitDepartment on table 51043
   Fields: Department Code, Limit
2. Portal will block requests above limit automatically

H) IMPREST / SURRENDER OData (optional but recommended)
-------------------------------------------------------
1. QyImprestSurrenderHeader: expose Balance, Balance_Less_this_Entry, Surrender Status
2. ImprestSurrenderHeader AL: Validate imprest link after create
3. Imprest line amount: FetchImprestLineAmount must use travel destination daily rate table

I) AFTER ALL BC PUBLISHES
-------------------------
1. Restart portal on 10.30.4.23
2. Log out and log in again
3. Run through UAT checklist one module at a time
