HIJRA 1.0.3.64 COMPLETE SUITE FINAL — ONE-TIME DEPLOY
======================================================

*** STEP 0 — FELIX FIRST (Business Central) ***
Send Felix the BC-AL/ folder from this zip (or deploy/HIJRA-FACILITY-AL-FOR-FELIX.zip).
He must compile, upload the .app, and publish ALL web services listed in:
  BC-AL/FELIX-PUBLISH-CHECKLIST.txt

Critical new SOAP services: CuPortalFacility, CuPortalAssetTransfer, CuPortalAttachments
Critical new OData queries: QyAssetTransfer, QyPortalFuelMaintExtra, QyWorkTicketFlight,
  QyProcurementPlanHeader, QyProcurementPlanLines, QyGatePassTransferShipments,
  QyGatePassAssetTransfers

Felix smoke test (must not 404) before portal deploy:
  .../QyPortalFuelMaintExtra?$top=1
  .../WS/HIJRA%20BANK/Codeunit/CuPortalFacility

*** STEP 1 — STOP portal on UAT ***

*** STEP 2 — BACKUP C:\TA\SelfServiceSuite ***

*** STEP 3 — EXTRACT zip ***
Copy SelfServiceSuite\ to C:\TA\SelfServiceSuite\
RESTORE your .env from backup — do NOT overwrite with .env.example

*** STEP 4 — START ***
Run START-HIJRA-PORTAL.bat

*** STEP 5 — VERIFY ***
Run VERIFY-DEPLOY.bat — all [OK]
Browser: http://10.30.4.23:4000/api/health → v1.0.3.64
Ctrl+F5 on login page

*** STEP 6 — FACILITY SMOKE TEST ***
  Purchase Requisition → add line with Specification → attach file → submit
  Fuel Request → detail shows vehicle/card/dealer/qty after create
  Transport → Field Trip stays Field Trip in approval queue
  Asset Transfer → separate from Transfer Order; post after approval
  Maintenance → template fields + assign technician
  Work Ticket → flight booking fields save
  Gate Pass → source document dropdown populated
  Store Requisition → receive button after store issues items

BUILD_ID=hijra-portal-1.0.3.64-FINAL-2026-07-27-facility-uat-final
