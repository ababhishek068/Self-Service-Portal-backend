HIJRA — BC ADMIN BABY STEPS (25 Jul 2026)
==========================================
The AL repo ("Hijra Al ERP V2/hijraERP/Hijra") has been updated with everything below.
Felix only needs to COMPILE + PUBLISH the extension once. Details are in
HIJRA-AL-CHANGES-2026-07-25.md at the AL project root.

STEP 1 — Open the AL project in VS Code on the BC machine
  Folder: hijraERP\Hijra

STEP 2 — Package + publish
  Ctrl+Shift+B (AL: Package), then F5 / AL: Publish extension.
  The install/upgrade codeunits AUTO-REGISTER these web services:
    CuPortalHrLetters, CuPortalTraining, CuPortalEmployeeExit,
    CuPortalEmployeeData, CuPortalAttachments,
    QyApprovalCommentLine, QyPettyCashLimitDepartment
  (No manual Web Services page entry needed.)

STEP 3 — One-time setup in BC web client
  Search "Portal Employee Transfer Setup":
    Immediate Supervisor User ID = fallback supervisor (e.g. KHALID)
    HR Approver User ID          = the HR user
  This HR user also approves the HR letters.

STEP 4 — Data setup (only if wanted)
  a) More staff claim types: "Receipts and Payment Types" -> rows with Type = Claim
  b) Petty cash limits: fill table "Petty Cash Limit-Department" (a row per department)

STEP 5 — Restart the portal once, everyone logs out/in, retest.

WHAT THE AL PUBLISH FIXES
  - HR Service Request Letters fully ERP-integrated (new table + codeunit + HR page)
  - Rejection reasons readable by the portal (QyApprovalCommentLine)
  - Petty cash department limit readable (QyPettyCashLimitDepartment)
  - Profile: Date of Birth + Marital Status now on QyHREmployee
  - Exit Interview no longer demands Transfer Type (UAT)
  - Transfer accepts Permanent / Temporary / Secondment + new typeOfTransfer field
  - Rejected transfer/resignation now returns the decision remarks to the portal
  - Claim header accepts claimDate + department (portal always sent them)
  - Training web service auto-registered (department/period/provider/cost work)
